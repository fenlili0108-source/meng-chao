# PROFILE_PAGE.md — 用户画像页 需求 & 生成 Prompt

> 配合 PRODUCT.md(§7 用户画像)、DESIGN.md(视觉)、INTERPRETATION_PROMPT.md(解读引擎)使用。
> 画像页是「越用越懂你」最集中的体现:它把散落在每次解读里的零散观察,
> 汇总成一份「系统目前怎么理解你」的总览,且用户可以修正。
>
> **核心机制(已和用户确认):画像内容由 AI 实时通览用户的全部梦境生成,追求准确度。**
> 不是平时攒下来的,是每次打开画像页时,把全部梦发给 AI 重新分析一次。

---

## 一、什么时候解锁

- 严格按 PRODUCT.md:**积累满 5 个梦才解锁画像页**。
- 不满 5 个梦时:导航里的「画像」入口显示为锁定态,
  点击或 hover 时提示「再记录 N 个梦,潜意识画像页就会解锁」(N = 5 减去当前梦数)。
- 满 5 个梦后:入口正常,可进入。

---

## 二、画像页的内容结构(从上到下)

> 关键设计原则:画像页提供的必须是**单次解读给不了的、只有"鸟瞰全部梦"才能得到的东西**。
> 不要把单次解读里说过的话重复一遍。三个板块各有独立价值:

### 板块 1:系统对你的理解(最顶部,最重要)
- 一句到两三句话,是 AI 通览全部梦后,对"这个用户内心核心议题"的整体判断。
  例:「目前我看到的你,内心有一条关于『方向与掌控』的主线——它时而表现为对外部威胁的警惕,
  时而是对自己选择的怀疑;但你也有能松弛、不设防的部分。」
- **用户可以对这句话操作:点「准」/「不准」/「修改」**(PRODUCT.md 的 system_understanding 可修正)。
  - 点「准」:记录一个正向反馈(以后可用于让画像更贴合)。
  - 点「不准」或「修改」:允许用户输入自己的修正,系统保存用户的版本。
  - 这个「用户能纠正系统」的动作是产品和算命的本质区别,务必做。
- 下方一行小字声明:「这是系统目前的理解,会随你记录更多而改变。」

### 板块 2:反复出现的母题(中间)
- 2–4 张卡片,每张是一个 AI 从全部梦里提炼的"反复处理的主题"。
  例:「在边缘试探」「努力却受挫的慌乱」「偶尔的、不设防的松弛」。
- 每张卡片:一个母题名 + 一句话说明 + (可选)它在哪几个梦里出现过。
- 卡片用 DESIGN.md 的卡片样式。

### 板块 3:统计概览(下方,用图表)
这是画像页独有的硬通货——**可量化的统计,单次解读永远做不到**。两个图表:

1. **情绪分布**:用一个**环形图(donut chart)**展示用户所有梦的情绪占比
   (如 焦虑/紧张 55%、平静/轻松 25%、其他 20%)。
2. **反复意象 / 主题频次**:用一个**横向条形图(horizontal bar)**展示出现最多的几个意象或主题
   (如 被追赶 ×4、找不到方向 ×3、旧物/怀念 ×1 ……)。

下方一行小字:「基于你目前记录的 N 个梦统计。」

---

## 三、图表的技术约束(给 Claude Code,降低翻车概率)

- **图表库用 Recharts**(`import { PieChart, Pie, Cell, BarChart, Bar, XAxis, YAxis } from "recharts"`)。
  它对深色主题友好、和 Next.js + React 契合度高。若有更合适的理由换库需先说明。
- **图表必须适配深色主题**,严格用 DESIGN.md 的颜色 token:
  - 图表的主色用 `--accent`(#8B7CF6)和 `--accent-blue`(#5B8DEF)及它们的透明度变体。
  - 背景透明 / 用 `--bg-elevated`,**绝对不要出现白色底的图表**。
  - 文字标签用 `--text-secondary`,网格线极淡或不要。
- 环形图中间留空,可放一个数字(如梦的总数)。
- 图表数据来自下面 AI 生成的画像 JSON,不要在前端硬编码假数据。
- **容错**:如果某项数据为空或 AI 没返回,图表区域显示「数据还不够,多记几个梦」,不要崩或显示空白图。

---

## 四、画像生成 Prompt(给 AI,实时通览全部梦)

> 用户打开画像页时,后端把该用户的**全部梦境 + 每次的情绪标记**拼进 user message,
> 配下面这段 system prompt,调用 DeepSeek(同解读引擎,deepseek-v4-flash,OpenAI 兼容格式)。
> **要求 AI 返回结构化 JSON**,方便前端把各部分填进对应板块和图表。

### System Prompt

```
你是一个解梦产品的「用户画像」分析引擎。用户已经记录了多个梦,现在要你通览他的全部梦境,
生成一份"系统目前怎么理解这个人"的总览。这份总览要提供单次解梦给不了的、只有鸟瞰全部才能得到的洞察。

你必须严格遵守解读引擎的同款铁律:
- 特异性:所有判断必须扎根于这个用户真实记录的梦,严禁放之四海的空话。
- 不脑补现实:可以总结梦里反复出现的模式、意象、情绪,但绝不替用户认领他没说过的现实生活动机、关系、事件。
- 不诊断、不预言:描述模式和情绪,不贴疾病标签,不预测未来。
- 流派藏在后台:用心理学的眼光分析,但不堆砌"荣格""弗洛伊德"等术语。
- 语气专业但温柔,像一个认真观察了你很久的朋友。

请只输出一个 JSON 对象,不要有任何额外文字、不要用 markdown 代码块包裹,格式如下:

{
  "understanding": "一到三句话,对用户内心核心议题的整体判断。扎根于具体的梦,不要空泛。",
  "motifs": [
    {"name": "母题名(简短)", "desc": "一句话说明", "dreams": "它出现在哪几个梦里的简短指代"},
    ... (2 到 4 个)
  ],
  "emotion_distribution": [
    {"label": "情绪类别(如 焦虑/紧张)", "percent": 数字(0-100,所有项加起来约等于100)},
    ... (按占比从高到低,合并相近情绪,一般 3-4 类)
  ],
  "motif_frequency": [
    {"label": "意象或主题名", "count": 出现次数(整数)},
    ... (按次数从高到低,取前 4-6 个)
  ]
}

注意:
- understanding 是最重要的,要让用户读了觉得"这确实是在说我",而不是说谁都行。
- 如果某个梦的情绪用户没标记,你根据梦的内容合理归类,但不要过度演绎。
- 数字要基于真实的梦,不要编造不存在的意象或夸大次数。
```

### User Message 怎么拼

```
以下是这个用户记录的全部梦境,请通览后按要求生成画像 JSON:

【梦 1】日期:… 内容:… 用户标记的情绪:…
【梦 2】日期:… 内容:… 用户标记的情绪:…
……(全部梦)

请输出画像 JSON。
```

### 接入提示(给 Claude Code)
- 把上面 system prompt 和拼好的 user message 发给 DeepSeek,拿回 JSON 后解析。
- **JSON 解析要容错**:DeepSeek 偶尔会在 JSON 外面包一层 ```json``` 或加句话,
  解析前先把代码块标记和多余文字去掉,只取 `{...}` 部分;解析失败时给友好提示,不要崩。
- 这段 prompt 同样放在 `prompts.ts` 之类的地方单独管理,方便迭代。
- 画像 prompt 也放进 INTERPRETATION 同款的、不硬编码散落的原则。

---

## 五、验收清单(你的活)

满 5 个梦后打开画像页,逐条看:
- [ ] **解锁逻辑对**:不满 5 个梦时锁定并提示;满 5 个梦后能进。
- [ ] **「系统对你的理解」准吗**:读起来是"在说我",还是套话?套话就是不合格。
- [ ] **能修正吗**:点「准/不准/修改」有反应,修改能保存。
- [ ] **母题对吗**:那 2-4 个母题,是不是真的从你的梦里来的、你认?
- [ ] **图表对吗**:① 情绪环形图、意象条形图都正常显示;② 是深色主题、不是白底;
      ③ 数字和你实际记的梦大致对得上(不是瞎编的)。
- [ ] **没重复解读**:画像页给的是"总览级"的洞察,不是把单次解读的话重说一遍。
- [ ] **没越界**:没有脑补你的现实生活、没下诊断、没预言。

任何一条不对,记下具体例子回来,我们一起调(可能是 prompt,也可能是前端)。
